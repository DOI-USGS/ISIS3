<?xml version="1.0" encoding="UTF-8"?>

<application name="topds4" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="http://isis.astrogeology.usgs.gov/Schemas/Application/application.xsd">
  <brief>
    dummy
  </brief>

  <description>
    dummy
  </description>

  <category>
    <categoryItem>Trim and Mask</categoryItem>
  </category>

  <history>
    <change name="Jeffrey Covington" date="2015-01-15">
      Removed unreachable code.
    </change>
  </history>

  <groups>
    <group name="Files">
      <parameter name="FROM">
        <type>cube</type>
        <fileMode>input</fileMode>
        <brief>
          Input cube
        </brief>
        <description>
          This is the input cube that will be cropped.
        </description>
        <filter>
          *.cub
        </filter>
      </parameter>

      <parameter name="TEMPLATE">
        <type>filename</type>
        <fileMode>input</fileMode>
        <brief>
          Input template
        </brief>
        <description>
          Input template
        </description>
      </parameter>

      <parameter name="TO">
        <type>filename</type>
        <fileMode>output</fileMode>
        <brief>
          Output PDS4 label
        </brief>
        <description>
          The.
        </description>
      </parameter>
    </group>

    <group name="Other">
      <parameter name="MAINLABEL">
        <type>boolean</type>
        <default><item>true</item></default>
        <brief>
          Use the main cube label as input data
        </brief>
        <description>
          Use the main input cube label as a source of data to replace template elements with.
        </description>
      </parameter>

      <parameter name="ORIGINALLABEL">
        <type>boolean</type>
        <default><item>false</item></default>
        <brief>
          Use the original label as input data
        </brief>
        <description>
          Use the original label attached to the input cube as a source of data 
          to replace template elements with. This is the label that was saved when
          the cube was first ingested into ISIS as a cube.
        </description>
      </parameter>


    </group>
  </groups>
</application>
