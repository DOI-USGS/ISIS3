import setuptools

if __name__ == '__main__':
    setuptools.setup(
        name='isiscore',
        version='1.0.0',
        packages=['isiscore'],
        package_data={'':['_isiscore.so']},
        zip_safe=False,  # Some OSes can not dynamically load an so from an egg (zipfile)
        license='UnLicense',
        author='arsanders/acpaquette',
        author_email='arsanders@usgs.gov'
)
