Improved spiceinit web error reporting where SpiceClient now parses JSON error responses ({"body": {"error": ...}}) from the spiceserver and surfaces the reported cause instead of discarding it behind a generic message.
