Improved spiceinit web SPICE error reporting: SpiceClient now parses JSON error responses ({"body": {"error": ...}}) from the spiceserver and includes the raw server response (which carries the actual cause and a request hash) instead of discarding it behind a generic message.
