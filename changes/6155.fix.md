Fixed `cam2map`'s USEPROJ option writing the misspelled `EquitorialRadius` keyword instead of `EquatorialRadius`, which caused the camera's default target radius to silently override the user-requested ellipsoid radius.
