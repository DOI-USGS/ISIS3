Refactored `blend` to be callable and migrated its legacy Makefile tests to gtest/CTest. Replaced the legacy ISISTESTDATA dependency with reduced repo-local fixtures, reducing blend test data footprint from 118 MB to 12 MB.
