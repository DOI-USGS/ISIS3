Fixed `cam2map`'s USEPROJ option silently corrupting or crashing the output ground range when given a PROJString in a format other than a raw PROJ4-style `+key=value` string (e.g. an authority code, WKT, or PROJJSON), by restricting the parameter to PROJ-string format only and adding an axis-order normalization in `IProj`.
