Fixed jigsaw failing to read cubes that have a CSM sensor model attached, which happened when two such cubes were given the same serial number. A cube that also has a recognized instrument label now keeps its unique instrument-based serial number instead of a CSM serial number that can be identical for multiple images.
