Add CSM parameter to phocube for loading a CSM camera (ISD or model state JSON) from an external file instead of the input cube. The cube does not need to be run through spiceinit in this mode. Mirrors the approach used for ISD/CSMLIST in cam2map and jigsaw. Issue #6034.
